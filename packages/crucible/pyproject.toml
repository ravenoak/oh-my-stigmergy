[project]
name = "oh-my-stigmergy-crucible"
version = "0.1.0"
description = "Deterministic Allium model to SMT-LIB compiler (FR-4.2)"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = []

[project.optional-dependencies]
dev = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
