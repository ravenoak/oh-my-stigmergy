[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "oh-my-stigmergy-transitions"
version = "0.1.0"
description = "Allium model transitions harness (FR-1.3)"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
