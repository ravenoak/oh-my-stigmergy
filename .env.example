# Copy to `.env` for local npm publish when not using GitHub OIDC (never commit `.env`).
# Preferred: Actions workflow `.github/workflows/npm-publish.yml` + npm Trusted Publishing (no token).
# npm automation tokens: https://docs.npmjs.com/about-access-tokens
#
# NPM_TOKEN=npm_your_token_here
