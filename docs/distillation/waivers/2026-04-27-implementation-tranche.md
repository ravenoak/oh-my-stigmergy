# Full implementation tranche — governance, graph, SBP, crucible scaffolding

- **Reason:** This change set adds `devtools/` scripts and contracts without expanding behavioural Allium entities in `spec/` beyond what is strictly required for traceability scripts; governance FRs are satisfied via docs + deterministic scripts.
- **Follow-up:** Domain behaviour should migrate into `spec/**/*.allium` as features are elicited; remove this waiver when the next distil pass updates specs for the touched prefixes.
