# Phase 11 — OpenCode plugin implementation (packages/opencode-plugin)

- **Reason:** Behaviour is specified in **FR-5.1–FR-5.3**, **ADR-0012**, and **governance** entities `OpenCodePluginTool` / `OpenCodePluginEvent` (landed in Phase 11 P11-a on `main`). This PR adds **implementation** and tests under `packages/opencode-plugin/` without a new reverse-distillation slice into `spec/`.
- **Follow-up:** Optional: extend Allium with operational detail if we want executable spec parity beyond FR/ADR + crucible fixtures; otherwise **none**.
