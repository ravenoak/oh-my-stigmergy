# PR 5 — Phase 5 depth + operability closeout

- **Reason:** This tranche deepens in-tree deterministic tooling (Crucible encoders/goldens, graph ingestion/retrieval depth, SBP compaction/decay GC, shim hardening) and updates FR/NFR/RTM/ADRs/CI contracts accordingly, but does not introduce new Allium behavioural modules under `spec/`.
- **Follow-up:** none (Phase 5 scope is implemented and evidenced via tests + RTM rather than new `spec/` obligations).

