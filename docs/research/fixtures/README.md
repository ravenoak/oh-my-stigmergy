# Research fixtures (non-secret)

| File | Purpose |
|------|---------|
| [lab-audit.ndjson](lab-audit.ndjson) | Synthetic audit lines for `summarize-audit.mjs` pipeline checks. |
| [orchestration.policy.example.json](orchestration.policy.example.json) | **Example only** — JSON matching [`packages/opencode-plugin/schema/orchestration.schema.json`](../../packages/opencode-plugin/schema/orchestration.schema.json) for **Condition B2** in [protocol 1.1+](../opencode-effectiveness-study-protocol.md). Replace `example-provider/…` strings with **real** OpenCode provider/model ids from your environment before use in a study run; the bundled ids are intentionally invalid for routing. |
