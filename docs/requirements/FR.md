# Functional requirements

**Convention:** IDs are stable. **Phase:** P0–P4 (see [docs/README.md](../README.md)). **Maturity:** `implemented` | `partial` | `planned`.

## Epic 1 — Elicitation and intent formalization (Allium)

| ID | Requirement | Phase | Maturity | Spec anchor | Notes |
|----|-------------|-------|----------|-------------|-------|
| FR-1.1 | The platform shall support bidirectional specification work in Allium (author, parse, evolve). | P1 | implemented | [`spec/project.allium`](../../spec/project.allium), [`spec/governance.allium`](../../spec/governance.allium) | JUXT skills; CI runs `allium check` and `allium analyse` on `spec/`; seed + governance module. |
| FR-1.2 | Provide distillation path: draft specs from code structure, surfacing gaps to the human architect. | P1 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`devtools/distillation-contract.json`](../../devtools/distillation-contract.json) | **`DistillationArtefact`** slice in [`spec/governance.allium`](../../spec/governance.allium); [Distillation playbook](../guides/distillation-playbook.md); `/allium:distill` per [juxt/allium](https://github.com/juxt/allium). **Deterministic (PRs):** [`scripts/verify-distillation-contract.sh`](../../scripts/verify-distillation-contract.sh). |
| FR-1.3 | State-dependent fields and transition graphs constrain allowed state changes; violations are treated as critical at maturity. | P2 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`spec/project.allium`](../../spec/project.allium) | **Deterministic today:** vendor CLI `allium model <file.allium>` merged across `spec/*.allium` by [`packages/transitions`](../../packages/transitions/) (`TransitionTable.from_allium_specs`); CI heavy job runs `packages/transitions/tests` with `allium` on `PATH`. |
| FR-1.4 | Agent stance configuration has a normative schema and validator. | P2 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`packages/stance/schema/stance-config.schema.json`](../../packages/stance/schema/stance-config.schema.json), [`packages/stance/src/stance/validate.py`](../../packages/stance/src/stance/validate.py), [`docs/adr/0010-stance-configuration-schema.md`](../adr/0010-stance-configuration-schema.md) | **`StanceTarget`** slice in [`spec/governance.allium`](../../spec/governance.allium); **Deterministic today:** `jsonschema` validation in `packages/stance` (`packages/stance/tests/test_validate.py`, `packages/stance/tests/test_cli.py` for `python -m stance.validate`); optional `SBP_STANCE_REGISTRY` in [`packages/sbp-server/server.mjs`](../../packages/sbp-server/server.mjs) (`stance-registry.test.mjs`). **Non-goal:** this repo does not run an agent runtime (ADR-0010). |

## Epic 2 — Topological navigation (relation-first graphs)

| ID | Requirement | Phase | Maturity | Spec anchor | Notes |
|----|-------------|-------|----------|-------------|-------|
| FR-2.1 | Ingestion chunks the repository into byte-addressed **code cards** (`char_start`/`char_end`). | P2 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`packages/graph/README.md`](../../packages/graph/README.md), [`tests/fixtures/graph-corpus/README.md`](../../tests/fixtures/graph-corpus/README.md) | **`CodeCard`** role/language slice in [`spec/governance.allium`](../../spec/governance.allium); [`packages/graph/`](../../packages/graph/) — Python + TypeScript + shell line cards; optional Tree-sitter **symbol** / **method** / **decorator** cards for Python (`tree-sitter-python`) and TypeScript (`tree-sitter-typescript`) when bindings are installed ([ADR-0007](../adr/0007-graph-persistence.md), [ADR-0002](../adr/0002-relation-first-retrieval.md)). CI: `uv sync` then `unittest`; `test_ingest_multilang.py`, `test_calls_edges.py`. |
| FR-2.2 | Background or batch process maintains **aspect graphs** (e.g. auth, monetary, architecture views). | P2 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`packages/graph/README.md`](../../packages/graph/README.md) | **`AspectEdge`** slice in [`spec/governance.allium`](../../spec/governance.allium); **IMPORTS** (Python/TS) + **SOURCES** (shell) + **CALLS** (Python/TS best-effort callee text) edges from [`graph/ingest.py`](../../packages/graph/src/graph/ingest.py); `uv run python -m graph.aspect`; `test_aspect_graph.py`, `test_aspect_cli.py`, `test_ingest_multilang.py`, `test_calls_edges.py`. SQLite: [ADR-0007](../adr/0007-graph-persistence.md). |
| FR-2.3 | Agents can call `load_node(id)` (or equivalent) returning only incident slices, not embedding noise. | P2 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`packages/graph/README.md`](../../packages/graph/README.md) | CLI `uv run python -m graph.load_node` with `--depth` and optional **`--edge-kind`** for bounded BFS over `IMPORTS`/`SOURCES`/`CALLS`; contract tests in `packages/graph/tests/test_load_node.py` + `test_calls_edges.py`; SQLite parity in `test_store_sqlite.py`. |

## Epic 3 — Stigmergic blackboard coordination (SBP)

| ID | Requirement | Phase | Maturity | Spec anchor | Notes |
|----|-------------|-------|----------|-------------|-------|
| FR-3.1 | Central semantic ledger with atomic updates and streaming (e.g. SSE) for observers. | P3 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`packages/sbp-server/README.md`](../../packages/sbp-server/README.md) | **`LedgerStore`** kind slice in [`spec/governance.allium`](../../spec/governance.allium); in-process ledger + SSE in Node; optional **JSONL** via `SBP_LEDGER_JSONL` / `JsonlLedgerStore` ([ADR-0008](../adr/0008-sbp-persistence.md)); optional **SQLite** via `SBP_LEDGER_SQLITE` / `SqliteLedgerStore` ([ADR-0011](../adr/0011-sbp-sqlite-store.md)); **compaction** + opt-in decay GC + size signal `SBP_LEDGER_MAX_BYTES` ([ADR-0009](../adr/0009-sbp-ledger-compaction-decay-gc.md), [ADR-0011](../adr/0011-sbp-sqlite-store.md)); `GET /healthz`; `bin/compact.mjs`; CI: `npm test` (`sqlite-store.test.mjs`, `healthz.test.mjs`, `multi-writer.test.mjs`, `durability.test.mjs`, …). |
| FR-3.2 | Tasks and states are **digital pheromones** with UUID, stance target, intensity, decay. | P3 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`packages/sbp-server/schemas/pheromone.json`](../../packages/sbp-server/schemas/pheromone.json), [`docs/operations/sbp-slo.md`](../operations/sbp-slo.md) | **`Pheromone.stance_target`** in [`spec/governance.allium`](../../spec/governance.allium); JSON Schema + `POST /pheromones`; **computed** `intensity` via [`intensity.mjs`](../../packages/sbp-server/intensity.mjs) + `inflations`; [`decay.test.mjs`](../../packages/sbp-server/test/decay.test.mjs), [`load.test.mjs`](../../packages/sbp-server/test/load.test.mjs), [`log-contract.test.mjs`](../../packages/sbp-server/test/log-contract.test.mjs); compaction + GC + SQLite tests; operator runbook [`sbp-operator-runbook.md`](../operations/sbp-operator-runbook.md). |
| FR-3.3 | UUID idempotency to prevent duplicate execution (“first claim wins”). | P3 | implemented | [`packages/sbp-server/README.md`](../../packages/sbp-server/README.md) | `POST /pheromones/:id/claim` returns 409 on duplicate claim; tests. |
| FR-3.4 | Pheromone floor / inflation prevents critical work from starving. | P3 | implemented | [`packages/sbp-server/README.md`](../../packages/sbp-server/README.md) | `POST /pheromones/:id/inflate`; tests in `packages/sbp-server/test/floor.test.mjs`. |

## Epic 4 — Sublation crucible (verification beyond LLM)

| ID | Requirement | Phase | Maturity | Spec anchor | Notes |
|----|-------------|-------|----------|-------------|-------|
| FR-4.1 | Intercept agent shell commands (e.g. PATH shims) and evaluate against policy; block disallowed commands. | P4 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`devtools/crucible-shim/README.md`](../../devtools/crucible-shim/README.md) | **`ShimAllowEntry`** slice in [`spec/governance.allium`](../../spec/governance.allium); **Deterministic today:** deny-by-default [`devtools/crucible-shim/wrap.sh`](../../devtools/crucible-shim/wrap.sh) + [`devtools/crucible-shim/policy_gate.py`](../../devtools/crucible-shim/policy_gate.py) + attested [`devtools/crucible-shim/policy.maintainer.json`](../../devtools/crucible-shim/policy.maintainer.json) with optional **`args_regex`** per allow row; optional **`CRUCIBLE_SHIM_AUDIT_LOG`** NDJSON on deny; governance runs [`scripts/verify-shim-policy.sh`](../../scripts/verify-shim-policy.sh) + PR [`scripts/verify-shim-policy-diff.sh`](../../scripts/verify-shim-policy-diff.sh); heavy job runs [`tests/crucible_shim_contract.sh`](../../tests/crucible_shim_contract.sh). **Not pursued:** org-wide PATH/OPA — [BACKLOG.md](../BACKLOG.md), [ADR-0004](../adr/0004-verification-stack-layering.md). Maintainer PATH prepend per README and [ADR-0006](../adr/0006-p4-crucible-execution.md). |
| FR-4.2 | Deterministic compilation of `.allium` to SMT-LIB (no LLM in translation). | P4 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`packages/crucible`](../../packages/crucible/), [`tests/fixtures/crucible/README.md`](../../tests/fixtures/crucible/README.md) | Governance **`spec/governance.allium`** exercises overlay **`tests/fixtures/crucible/invariants.overlay.json`** in fixtures; **deterministic today:** [`packages/crucible`](../../packages/crucible/) compiles `allium model` JSON to SMT-LIB (**QF_UFLIA**; transition graphs + enum-only fields + bool + `required` + optional **`defaults` / `invariants`** merged from **`<stem>.overlay.json`**); Phase 9 crucible-only JSON adds **`int_range` / `int_eq` / `int_gt` / `int_lt`** on `Int` fields and **`card_in_range` / `card_eq`** on `List[…]` cardinality (`card_*` symbols) per [ADR-0006](../adr/0006-p4-crucible-execution.md) decision (5); [`scripts/verify-crucible-compile.sh`](../../scripts/verify-crucible-compile.sh) `diff`s `transitions` / `enums` / `required_fields` / `invariants` / `invariants_bad` / **`int_ranges` / `int_ranges_bad` / `collections` / `collections_bad` / `opencode_plugin` / `opencode_plugin_bad`** goldens; `unittest` under `packages/crucible/tests`; [`scripts/verify-smt-golden.sh`](../../scripts/verify-smt-golden.sh) runs `z3` on all `*.smt2` fixtures. |
| FR-4.3 | Integrations must pass satisfiability checking; unsat cores become explainable traces. | P4 | implemented | [`packages/crucible`](../../packages/crucible/), [`scripts/verify-smt-golden.sh`](../../scripts/verify-smt-golden.sh) | **Deterministic today:** `uv run python -m crucible.cli solve spec/` in `allium-specs` heavy job; `:named` assertions from `compile_named_model`; [`packages/crucible/tests/test_solve.py`](../../packages/crucible/tests/test_solve.py) covers sat + unsat-core explanation (enum-only, required JSON, **`invariants_bad`**, **`int_ranges_bad`**, **`collections_bad`** unsat / core); **`opencode_plugin_bad`** unsat; **sat** regression for `opencode_plugin` JSON model. |

## Epic 5 — Cognitive layer integration (OpenCode plugin)

| ID | Requirement | Phase | Maturity | Spec anchor | Notes |
|----|-------------|-------|----------|-------------|-------|
| FR-5.1 | The platform shall ship an installable **OpenCode plugin** package under [`packages/opencode-plugin`](../../packages/opencode-plugin/) (`@oh-my-stigmergy/opencode-plugin`, ESM) loadable per [OpenCode plugins](https://opencode.ai/docs/plugins/) (project dir, global dir, or `opencode.json` npm list). | P3 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`packages/opencode-plugin/README.md`](../../packages/opencode-plugin/README.md) | **Deterministic today:** [`scripts/verify-opencode-plugin-contract.sh`](../../scripts/verify-opencode-plugin-contract.sh) in `allium-specs` **governance** (manifest + **`npm pack`** tarball contains `package/src/index.mjs` + `private: false`); `packages/opencode-plugin/package.json` **`exports["."]` → `./src/index.mjs`**, **`scripts.metrics` → `node bin/metrics.mjs`**; `cd packages/opencode-plugin && npm ci && npm test` in **specs-and-packages** (includes orchestration tests). Optional **`STIGMERGY_AUDIT_LOG_FILE`** NDJSON audit (`src/auditLog.mjs`). **Product stance:** recommended OpenCode path is this plugin + SBP — [ADR-0013](../adr/0013-stigmergic-opencode-orchestration.md). |
| FR-5.2 | The plugin shall expose **custom tools** that validate arguments (Zod), call the SBP HTTP API (`POST/GET /pheromones`, claim, inflate), and return structured errors instead of crashing the session when SBP is down or inputs are invalid. | P3 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`packages/opencode-plugin/README.md`](../../packages/opencode-plugin/README.md) | **Deterministic today:** same `npm test` as FR-5.1; tools `stigmergy_publish`, `stigmergy_claim`, `stigmergy_inflate`, `stigmergy_pheromones`, `graph_load_node`, `graph_aspect` in [`packages/opencode-plugin/src/tools.mjs`](../../packages/opencode-plugin/src/tools.mjs) (`tool.schema.safeParse` + SBP client + `uv run` graph CLIs); `tool_execute` audit lines when `STIGMERGY_AUDIT_LOG_FILE` is set; [`packages/opencode-plugin/bin/metrics.mjs`](../../packages/opencode-plugin/bin/metrics.mjs) summarizes audit logs offline. |
| FR-5.3 | The plugin shall subscribe to OpenCode **events** (`session.idle`, `file.edited`), emit corresponding pheromones to SBP when configured, and log via `client.app.log` (fail-soft if `SBP_URL` unreachable). `file.edited` publishing is debounced per-path (trailing edge) to avoid one pheromone per keystroke/formatter pass. | P3 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`packages/opencode-plugin/README.md`](../../packages/opencode-plugin/README.md) | **`OpenCodePluginEvent.debounced`** field in [`spec/governance.allium`](../../spec/governance.allium); **Deterministic today:** [`packages/opencode-plugin/test/events.test.mjs`](../../packages/opencode-plugin/test/events.test.mjs) + [`packages/opencode-plugin/src/events.mjs`](../../packages/opencode-plugin/src/events.mjs) (per-path debounce via injectable timers, default `STIGMERGY_FILE_EDITED_DEBOUNCE_MS=3000`, `0` = immediate); [`packages/opencode-plugin/test/plugin.test.mjs`](../../packages/opencode-plugin/test/plugin.test.mjs) fail-soft path; structured NDJSON for event hook + SBP publish attempts (including `file_edited_debounced` coalescing events) when **`STIGMERGY_AUDIT_LOG_FILE`** is set (see README). |
| FR-5.4 | The project shall provide a **documented operator path** from clone through SBP, OpenCode plugin configuration, and graph verification, with root-level links for discoverability. | P3 | implemented | [`docs/guides/opencode-stigmergy-golden-path.md`](../guides/opencode-stigmergy-golden-path.md), [LICENSE](../../LICENSE) | **Deterministic today:** [`scripts/verify-opencode-golden-path.sh`](../../scripts/verify-opencode-golden-path.sh) in `allium-specs` **governance**; guide references `SBP_URL`, `SBP_LOG_FILE`, `STIGMERGY_AUDIT_LOG_FILE`, and `graph.load_node`; [README.md](../../README.md), [AGENTS.md](../../AGENTS.md), [CONTRIBUTING.md](../../CONTRIBUTING.md) link the guide; repository **MIT** [LICENSE](../../LICENSE); **`private: false`** on plugin package with **`npm pack`** contract per [ADR-0012](../adr/0012-opencode-plugin-architecture.md). |
| FR-5.5 | When **`SBP_URL`** is **unset**, the OpenCode plugin shall **attach or spawn** a **project-local** [`@oh-my-stigmergy/sbp-server`](../../packages/sbp-server); when **`SBP_URL`** is set, it shall **only attach**. Ephemeral **`PORT`**, **`.stigmergy/runtime.json`**, and **`STIGMERGY_SUPERVISE`** escape hatch per [ADR-0014](../adr/0014-sbp-project-supervision.md). | P3 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`packages/opencode-plugin/README.md`](../../packages/opencode-plugin/README.md), [ADR-0014](../adr/0014-sbp-project-supervision.md) | **Deterministic today:** [`packages/opencode-plugin/src/superviseSbp.mjs`](../../packages/opencode-plugin/src/superviseSbp.mjs); [`packages/opencode-plugin/test/superviseSbp.test.mjs`](../../packages/opencode-plugin/test/superviseSbp.test.mjs); `npm test` in **specs-and-packages**; [`packages/sbp-server/test/supervised-runtime.test.mjs`](../../packages/sbp-server/test/supervised-runtime.test.mjs); [`scripts/verify-opencode-plugin-contract.sh`](../../scripts/verify-opencode-plugin-contract.sh) (`@oh-my-stigmergy/sbp-server` registry semver in [`packages/opencode-plugin/package.json`](../../packages/opencode-plugin/package.json)); [`scripts/verify-opencode-plugin-publishable.sh`](../../scripts/verify-opencode-plugin-publishable.sh) in **governance** (Phase **P19-a**); [`scripts/verify-stigmergy-sbp-supervision-doc.sh`](../../scripts/verify-stigmergy-sbp-supervision-doc.sh) in **governance**. |

## Epic 6 — Stigmergic OpenCode orchestration

| ID | Requirement | Phase | Maturity | Spec anchor | Notes |
|----|-------------|-------|----------|-------------|-------|
| FR-6.1 | The OpenCode plugin shall provide **stigmergic orchestration** tools: resolve **OpenCode model id** from **stance** via JSON policy (`STIGMERGY_ORCHESTRATION_CONFIG` or defaults), and list **actionable** pheromones from SBP filtered by **olfactory threshold** (computed intensity) with optional stance filter. | P3 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`packages/opencode-plugin/schema/orchestration.schema.json`](../../packages/opencode-plugin/schema/orchestration.schema.json), [ADR-0013](../adr/0013-stigmergic-opencode-orchestration.md) | **Deterministic today:** [`packages/opencode-plugin/src/orchestration.mjs`](../../packages/opencode-plugin/src/orchestration.mjs); tools `stigmergy_resolve_model`, `stigmergy_actionable` in [`packages/opencode-plugin/src/tools.mjs`](../../packages/opencode-plugin/src/tools.mjs); [`packages/opencode-plugin/test/orchestration.test.mjs`](../../packages/opencode-plugin/test/orchestration.test.mjs) + [`packages/opencode-plugin/test/tools-schema.test.mjs`](../../packages/opencode-plugin/test/tools-schema.test.mjs); plugin `npm test` in **specs-and-packages**. |
| FR-6.2 | The project shall document **migration from oh-my-openagent**, link **ADR-0013** from root docs, and supply a **bootstrap** script for SBP + graph + plugin dependencies. | P3 | implemented | [ADR-0013](../adr/0013-stigmergic-opencode-orchestration.md), [`docs/guides/migration-from-oh-my-openagent.md`](../guides/migration-from-oh-my-openagent.md) | **Deterministic today:** [`scripts/verify-stigmergy-orchestration-doc.sh`](../../scripts/verify-stigmergy-orchestration-doc.sh) in `allium-specs` **governance**; [`scripts/bootstrap-opencode-stigmergy-stack.sh`](../../scripts/bootstrap-opencode-stigmergy-stack.sh); [README.md](../../README.md), [AGENTS.md](../../AGENTS.md), [CONTRIBUTING.md](../../CONTRIBUTING.md) link ADR-0013 + migration guide. |
| FR-6.3 | The project shall supply **operator documentation** for OpenCode model routing, a **compatibility matrix** (pinned `@opencode-ai/plugin`), and an **npm release runbook**; root docs shall link them; compatibility doc shall stay aligned with the peer dependency version. | P3 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`docs/guides/opencode-model-routing-playbook.md`](../guides/opencode-model-routing-playbook.md), [`docs/operations/opencode-compatibility.md`](../operations/opencode-compatibility.md), [`docs/operations/opencode-plugin-release.md`](../operations/opencode-plugin-release.md) | **Deterministic today:** [`scripts/verify-opencode-operator-docs.sh`](../../scripts/verify-opencode-operator-docs.sh) in `allium-specs` **governance**; [`packages/opencode-plugin/test/orchestration.test.mjs`](../../packages/opencode-plugin/test/orchestration.test.mjs) + [`packages/opencode-plugin/test/tools-schema.test.mjs`](../../packages/opencode-plugin/test/tools-schema.test.mjs) for policy-bound actionable defaults ([Phase 15](../ROADMAP.md)). **Other:** OIDC **trusted publishing** workflow [`.github/workflows/npm-publish.yml`](../../.github/workflows/npm-publish.yml) ([npm docs](https://docs.npmjs.com/trusted-publishers/), runbook). |
| FR-6.4 | The project shall maintain **operator documentation** for **project positioning and normative boundaries** (multi-agent, verification scope, decentralization, reference deployment scale) and for **stigmergic SDLC workflow patterns**; **inspiration errata** for non-normative essay drift; root and documentation index shall link the guides; CI shall verify required guide sections. | P3 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`docs/guides/project-positioning-and-boundaries.md`](../guides/project-positioning-and-boundaries.md), [`docs/guides/stigmergic-sdlc-workflows.md`](../guides/stigmergic-sdlc-workflows.md), [`docs/inspiration-errata.md`](../inspiration-errata.md) | **Deterministic today:** [`scripts/verify-project-positioning-doc.sh`](../../scripts/verify-project-positioning-doc.sh), [`scripts/verify-stigmergic-sdlc-workflows-doc.sh`](../../scripts/verify-stigmergic-sdlc-workflows-doc.sh) in `allium-specs` **governance**; [`tests/ci_contract.sh`](../../tests/ci_contract.sh) locks scripts + paths ([Phase 16](../ROADMAP.md)). |
| FR-6.5 | The project shall maintain **operator troubleshooting documentation** for the OpenCode + SBP + graph stack (connectivity, bridge error prefixes, `uv`/Python, orchestration policy); the **golden path** shall link it; CI shall verify required sections and cross-links. | P3 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`docs/operations/opencode-stigmergy-troubleshooting.md`](../operations/opencode-stigmergy-troubleshooting.md) | **Deterministic today:** [`scripts/verify-opencode-stigmergy-troubleshooting-doc.sh`](../../scripts/verify-opencode-stigmergy-troubleshooting-doc.sh) in `allium-specs` **governance**; [`scripts/verify-opencode-golden-path.sh`](../../scripts/verify-opencode-golden-path.sh) requires link to troubleshooting; [`tests/ci_contract.sh`](../../tests/ci_contract.sh) ([Phase 17](../ROADMAP.md)). |

## Epic 7 — Empirical discipline (evaluation)

| ID | Requirement | Phase | Maturity | Spec anchor | Notes |
|----|-------------|-------|----------|-------------|-------|
| FR-7.1 | The project shall maintain **operator-facing documentation** for **empirical discipline**: what CI proves vs what requires controlled studies; **falsifiable hypotheses** for stigmergy patterns; explicit **non-claims** aligned with [ADR-0004](../adr/0004-verification-stack-layering.md); [docs/README.md](../README.md) shall link the guide. | P3 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`docs/guides/stigmergy-evaluation-discipline.md`](../guides/stigmergy-evaluation-discipline.md), [CONSTITUTION.md](../CONSTITUTION.md) | **Deterministic today:** [`scripts/verify-stigmergy-evaluation-discipline-doc.sh`](../../scripts/verify-stigmergy-evaluation-discipline-doc.sh) in `allium-specs` **governance**; [`tests/ci_contract.sh`](../../tests/ci_contract.sh); [`scripts/verify-requirement-traceability.sh`](../../scripts/verify-requirement-traceability.sh) ([Phase 19](../ROADMAP.md)). |
| FR-7.2 | The project shall maintain a **versioned OpenCode effectiveness study protocol** (tasks, metrics, conditions **A / B1 / B2**, reporting rules) and **deterministic** audit summarization helpers for operator-run evaluations on real code; [docs/README.md](../README.md) shall link the research index. | P3 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`docs/research/opencode-effectiveness-study-protocol.md`](../research/opencode-effectiveness-study-protocol.md), [`docs/research/FEASIBILITY_PILOT_STATUS.md`](../research/FEASIBILITY_PILOT_STATUS.md), [ADR-0015](../adr/0015-empirical-evaluation-study-claims.md) | **Deterministic today:** [`scripts/verify-opencode-evaluation-protocol.sh`](../../scripts/verify-opencode-evaluation-protocol.sh) in `allium-specs` **governance**; [`devtools/evaluation/README.md`](../../devtools/evaluation/README.md) + [`devtools/evaluation/summarize-audit.mjs`](../../devtools/evaluation/summarize-audit.mjs); [`tests/ci_contract.sh`](../../tests/ci_contract.sh); [`scripts/verify-requirement-traceability.sh`](../../scripts/verify-requirement-traceability.sh). **Other:** human-run pilots; optional results under `docs/research/results/`; explicit pilot status in [`docs/research/FEASIBILITY_PILOT_STATUS.md`](../research/FEASIBILITY_PILOT_STATUS.md) ([Phase 20](../ROADMAP.md), Phase 20 follow-on). |

## Epic 8 — Artifact treaties (contract hardening, no behavior change)

| ID | Requirement | Phase | Maturity | Spec anchor | Notes |
|----|-------------|-------|----------|-------------|-------|
| FR-8.1 | A committed `transitions.json` mirrors every transition graph declared in `spec/*.allium`, drift-checked in CI. | P2 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`spec/transitions.json`](../../spec/transitions.json), [`packages/transitions/schema/transitions.schema.json`](../../packages/transitions/schema/transitions.schema.json) | **`TransitionArtifact`** slice in [`spec/governance.allium`](../../spec/governance.allium); **Deterministic today:** `python -m transitions --spec-dir spec --output spec/transitions.json` ([`packages/transitions/src/transitions/artifact.py`](../../packages/transitions/src/transitions/artifact.py)); golden-diff [`scripts/verify-transitions-golden.sh`](../../scripts/verify-transitions-golden.sh) (crucible-compile pattern); `packages/transitions/tests/test_artifact.py`. Mirrors whatever state machines exist in `spec/` today (governance workflows) — does **not** assume an SDLC phase taxonomy; see [`docs/planning/orchestrator-implementation-plan.md`](../planning/orchestrator-implementation-plan.md) §7. |
| FR-8.2 | The graph package's SQLite store carries an explicit, versioned schema contract (`PRAGMA user_version`), rejecting databases newer than the code supports. | P2 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`packages/graph/README.md`](../../packages/graph/README.md) | **`GraphStoreSchema`** slice in [`spec/governance.allium`](../../spec/governance.allium); **Deterministic today:** `graph.store.SCHEMA_VERSION` stamped by `SqliteCardStore.init_schema()` after additive column migrations; `packages/graph/tests/test_schema_version.py` (fresh DB, pre-versioning DB migration, future-version rejection). |
| FR-8.3 | A shared valid/invalid stance-config fixture corpus is consumed by both the Python `jsonschema` validator tests and the JS `stance-registry` tests, documenting where their contracts diverge. | P2 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`packages/stance/README.md`](../../packages/stance/README.md) | **`StanceFixture`** slice in [`spec/governance.allium`](../../spec/governance.allium); **Deterministic today:** [`tests/fixtures/stance/good.json`](../../tests/fixtures/stance/good.json) + [`tests/fixtures/stance/invalid/`](../../tests/fixtures/stance/invalid/) (4 fixtures) consumed by `packages/stance/tests/test_validate.py` + `test_cli.py` (strict) and [`packages/sbp-server/test/stance-registry.test.mjs`](../../packages/sbp-server/test/stance-registry.test.mjs) (permissive — only requires a `stance_vector` object). |

## Epic 9 — Ledger identity and kind (reduced scope)

| ID | Requirement | Phase | Maturity | Spec anchor | Notes |
|----|-------------|-------|----------|-------------|-------|
| FR-9.1 | The SBP ledger shall support opt-in identity tokens (`worker`/`privileged` classes, server-stamped `agentId`) and a `kind` field with an opt-in, class-gated registry, while preserving exact single-operator open-mode behavior when neither is configured. | P3 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [ADR-0016](../adr/0016-sbp-ledger-identity-and-kind.md) | **`LedgerIdentity`** slice + **`Pheromone.kind`** field in [`spec/governance.allium`](../../spec/governance.allium); **Deterministic today:** `SBP_AUTH_TOKENS_FILE` / `SBP_KIND_REGISTRY_FILE` / `SBP_INFLATE_MAX_PER_WINDOW` / `SBP_INFLATE_WINDOW_SECONDS` in [`packages/sbp-server/server.mjs`](../../packages/sbp-server/server.mjs) (`loadAuthTokens`, `loadKindRegistry`); [`packages/sbp-server/test/auth.test.mjs`](../../packages/sbp-server/test/auth.test.mjs) (auth matrix, kind-registry class gating, inflate budget, explicit open-mode regression suite); migration backstamp verified in [`packages/sbp-server/test/compaction.test.mjs`](../../packages/sbp-server/test/compaction.test.mjs). Plugin: `STIGMERGY_AGENT_TOKEN` in [`packages/opencode-plugin/src/index.mjs`](../../packages/opencode-plugin/src/index.mjs); `auth_error:`/`kind_unregistered` pass-through in [`packages/opencode-plugin/src/tools.mjs`](../../packages/opencode-plugin/src/tools.mjs) ([`packages/opencode-plugin/test/plugin.test.mjs`](../../packages/opencode-plugin/test/plugin.test.mjs), [`packages/opencode-plugin/test/tools-schema.test.mjs`](../../packages/opencode-plugin/test/tools-schema.test.mjs)). **Not pursued here:** `workOrder`/`phaseTransition`/`verificationMark` kinds — deferred to their first real consumer per [`docs/planning/orchestrator-implementation-plan.md`](../planning/orchestrator-implementation-plan.md). |

## Epic 10 — Delivery floors (report-only)

| ID | Requirement | Phase | Maturity | Spec anchor | Notes |
|----|-------------|-------|----------|-------------|-------|
| FR-10.1 | A frozen-test-manifest check warns when a PR changes a registered test path's content without an explicit amendment label. | P0 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`devtools/frozen-test-manifest.json`](../../devtools/frozen-test-manifest.json) | **`DeliveryFloorCheck`** slice in [`spec/governance.allium`](../../spec/governance.allium); **Deterministic today (report-only):** [`scripts/verify-frozen-test-manifest.sh`](../../scripts/verify-frozen-test-manifest.sh) — always exits 0 unless `DELIVERY_FLOORS_BLOCKING=1`; `test-amendment` label escape. Phase-keying is interim (manifest `frozenPaths` entries ARE the phase declaration; no ledger `phaseTransition` mark exists yet) — see [`docs/planning/orchestrator-implementation-plan.md`](../planning/orchestrator-implementation-plan.md). Ships with `frozenPaths: {}` (nothing frozen yet). |
| FR-10.2 | A lockfile-freeze check warns when a PR changes a lockfile without its sibling manifest, absent a review label. | P0 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`devtools/lockfile-freeze.json`](../../devtools/lockfile-freeze.json) | **Deterministic today (report-only):** [`scripts/verify-lockfile-freeze.sh`](../../scripts/verify-lockfile-freeze.sh); `deps-review` label escape. |
| FR-10.3 | A dependency-allowlist check warns when a PR adds a new dependency name not on the allowlist for its ecosystem. | P0 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`devtools/dependency-allowlist.json`](../../devtools/dependency-allowlist.json) | **Deterministic today (report-only):** [`scripts/verify-dependency-allowlist.sh`](../../scripts/verify-dependency-allowlist.sh) diffs manifest dependency names between merge-base and `HEAD` (pip via `tomllib`, npm via `package.json`); `deps-review` label escape; version bumps of already-allowed deps are not flagged. |
| FR-10.4 | A fixture-PR test harness exercises co-touch-style `verify-*.sh` scripts (including the pre-existing ones) against synthetic git state, since they resolve `origin/$GITHUB_BASE_REF` and previously required a real PR to test. | P0 | implemented | [`tests/lib/fixture_pr.sh`](../../tests/lib/fixture_pr.sh), [`tests/delivery_floors_contract.sh`](../../tests/delivery_floors_contract.sh) | **Deterministic today:** fakes `refs/remotes/origin/<branch>` (no real remote/network needed); copies the script-under-test into the fixture so its self-located `repo_root` resolves there; includes a harness self-check against `verify-governance-doc-cotouch.sh` proving fidelity, not just asserting new-script behavior. |

## Epic 11 — WorkOrder payload profile and the human-as-orchestrator bridge

| ID | Requirement | Phase | Maturity | Spec anchor | Notes |
|----|-------------|-------|----------|-------------|-------|
| FR-11.1 | The SBP server validates `payload` against a versioned WorkOrder profile whenever `kind == "workOrder"`, and cross-checks `provenance.createdBy` against the resolved identity when auth mode is on, regardless of open/auth mode for the structural check. | P3 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`packages/sbp-server/schemas/workorder-profile.schema.json`](../../packages/sbp-server/schemas/workorder-profile.schema.json) | **`WorkOrderProfile`** slice + **`Pheromone.kind`** extended (`workOrder`/`phaseTransition`/`verificationMark`) in [`spec/governance.allium`](../../spec/governance.allium); **Deterministic today:** hand-rolled `validateWorkOrderProfile()` in [`packages/sbp-server/server.mjs`](../../packages/sbp-server/server.mjs) (matches the house `validate()` convention — no new schema-validation dependency); [`packages/sbp-server/test/workorder-profile.test.mjs`](../../packages/sbp-server/test/workorder-profile.test.mjs) (13 tests: structural validation, open-mode pass-through, provenance cross-check + spoofing rejection, kind-registry gating composes after profile validation). `phase` is a free-form string, not a closed enum — no SDLC phase taxonomy exists yet ([`docs/planning/orchestrator-implementation-plan.md`](../planning/orchestrator-implementation-plan.md) §7). |
| FR-11.2 | A human-as-orchestrator bridge (small CLIs) lets a maintainer manually publish work orders and phase marks and check phase-close against a real, identity-mode SBP server, logging every action with per-session wall-clock to feed the orchestrator decision pre-registration. | P3 | implemented | [`devtools/bridge/README.md`](../../devtools/bridge/README.md) | **`BridgeSession`** slice in [`spec/governance.allium`](../../spec/governance.allium); **Deterministic today:** `session-start.mjs`/`session-end.mjs`/`publish-workorder.mjs`/`mark-phase.mjs`/`check-close.mjs` in [`devtools/bridge/`](../../devtools/bridge/); network-touching logic (`publishWorkOrder`/`markPhase`/`checkClose` in `lib.mjs`) tested in-process against a real SBP server; argv-validation paths tested via the real CLI subprocess in [`devtools/bridge/test/bridge.test.mjs`](../../devtools/bridge/test/bridge.test.mjs) (10 tests). This is the first real consumer that unlocks `workOrder`/`phaseTransition`/`verificationMark` in the kind registry ([`devtools/bridge/sbp-kind-registry.example.json`](../../devtools/bridge/sbp-kind-registry.example.json)). Deliberately `.mjs`, not Python — the runtime question belongs to workstream E if it is ever reached. |

## P0 repository governance (derived)

| ID | Requirement | Phase | Maturity | Spec anchor | Notes |
|----|-------------|-------|----------|-------------|-------|
| FR-0.1 | Behavioural changes to declared scope update Allium specs and RTM per [CONSTITUTION.md](../CONSTITUTION.md). | P0 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`spec/project.allium`](../../spec/project.allium) | **`RepositoryGovernance`** slice in [`spec/governance.allium`](../../spec/governance.allium); **Deterministic (PRs):** [`scripts/verify-governance-doc-cotouch.sh`](../../scripts/verify-governance-doc-cotouch.sh), [`scripts/verify-constitution-amendment-cotouch.sh`](../../scripts/verify-constitution-amendment-cotouch.sh). **Deterministic (push/PR):** [`scripts/verify-fr-spec-anchors.sh`](../../scripts/verify-fr-spec-anchors.sh). **Other:** human + agent review. |
| FR-0.2 | Specification files validate with `allium check` before merge when CI exists. | P0 | implemented | [`spec/governance.allium`](../../spec/governance.allium), [`spec/project.allium`](../../spec/project.allium) | **`WorkflowJob`** slice in [`spec/governance.allium`](../../spec/governance.allium); **Mechanical:** [`.github/workflows/allium-specs.yml`](../../.github/workflows/allium-specs.yml) splits work into **`filter`** (path relevance), **`governance`** (RTM, co-touch, anchors, distillation contract, `tests/ci_contract.sh`, SHA-pinned third-party actions via [`scripts/verify-actions-pinned.sh`](../../scripts/verify-actions-pinned.sh), job wall-clock bounds via [`scripts/verify-job-timeouts.sh`](../../scripts/verify-job-timeouts.sh) and [`devtools/ci-job-timeouts.json`](../../devtools/ci-job-timeouts.json) — [NFR-P2](NFR.md), every `scripts/verify-*.sh` is CI-wired or explicitly waived via [`scripts/verify-ci-contract-coverage.sh`](../../scripts/verify-ci-contract-coverage.sh) and [`devtools/verify-script-waivers.json`](../../devtools/verify-script-waivers.json)), **`specs-and-packages`** (pinned `allium-cli`, `allium check`/`analyse` on all `spec/**/*.allium`, graph + SBP tests, Z3 golden + crucible shim), and **`check`** (aggregating merge gate that preserves the required status name **`allium-specs / check`**). **Quota:** on pull requests, `specs-and-packages` is skipped when the diff is doc-only (paths under `docs/` except `docs/requirements/`, `docs/traceability/`, `docs/adr/`, plus root `*.md` only); pushes to `main` always run the heavy job. Cargo install is cached via `actions/cache@v4` keyed on [`devtools/allium-cli.version`](../../devtools/allium-cli.version). **Organizational:** default branch requires the `allium-specs` / `check` status per [docs/operations/github-branch-protection.md](../operations/github-branch-protection.md); maintainers apply with [`scripts/apply-branch-protection-main.sh`](../../scripts/apply-branch-protection-main.sh) (body [`devtools/branch-protection.json`](../../devtools/branch-protection.json)) and audit with [`scripts/verify-branch-protection-remote.sh`](../../scripts/verify-branch-protection-remote.sh). |

See [traceability/RTM.md](../traceability/RTM.md) for verification mapping.
